# 工作日报 · 2026-08-20

## 今天做了什么

**主线：`c-42f20d`（1688/consulting 合规整改，全公司唯一红线级P0）出整改清单——验收标准①已完成**

按用户指令"推进 c-42f20d，先看 1688 和 consulting 的整改范围"，派出 compliance-officer 只读排查两仓库全部对外可达内容（未执行任何改动，未做下线/改写的决定——那是 Eric 的权限，验收标准②明确写着"用户就每条决定"）。

结果：1688 与 consulting 各13条，合计**26条**file:line级不实内容/合规缺口，整理进 `docs/20260820-1688-consulting-合规整改清单.md`。核实并钉死了 `docs/20260816-家底盘点与经营决策.md` 第五节原本提到的问题的精确位置，并且多挖出不少原清单没有的：

- **1688伪造监管资质**（`warehouse-demo/index.html:345-370`）：虚构EORI号 `IE3829471T`、虚构"Revenue Customs Approval: ACTIVE"——不是营销夸大，是冒充爱尔兰税务/海关审批状态
- **consulting两个虚构办公室**（`app/[locale]/contact/page.tsx:7-18`）：伦敦Mayfair+香港中环，其中电话号码用了英国Ofcom预留的虚构号段 `020 7946`——技术上可直接证伪
- **consulting非locale版联系页**（`app/contact/page.tsx:6-21`）和locale版用的是**两套完全不同**的假地址/电话——虚构内容内部自相矛盾，本身就是证据
- **4处假表单正在丢真实数据**：1688两处（`local-warehouse-demo/index.html`、`warehouse-demo/index.html`）+ consulting两处（locale/非locale版contact页）——全部只是弹窗"已收到"，零后端接收，客户提交的信息静默丢失。这4条紧迫度最高，因为是唯一"每天都在造成实际损失"的一类，不是历史遗留问题
- consulting 10个虚构案例（`messages/en.json` cases.items，含精确编造的吞吐量/满意度/节省金额数字）和3处复用同一批数字的服务页"stats"板块——模板化伪造的证据
- 顺带核实：demos.html标题"这些都是实际运营中的系统，不是模拟数据"这句话本身就是不实陈述（部分卡片确实虚构）

清单里给每条标了A(下线)/B(改写)/C(加免责声明)三选一的建议选项，但**决定权在Eric**，我不代为拍板。

看板卡 `c-42f20d` 记了进度note，evidence指向新清单文档，同时打了 `--block`：等Eric逐条拍板后再排dev执行（当前dev WIP两个槽位仍被 `c-91a2d9`/`c-bb762f` 占满，且下一步性质是决策不是编码，暂不需要抢槽位）。

## 清单外顺带发现（未处理，已记录）

- veggie/Johnstone Bros 应收台账：`docs/20260816-对外资产合规排查.md` 说€3,000已收，`finance/ledger.jsonl` 显示€1,700应收，两者对不上——与本次26条无关（那是金额真实性问题不是虚构身份问题），需要另开一次核对
- `_archive/` 目录下约9个可能仍在跑的Cloud Run实例（含1688条目13的beauty demo，GCP项目 `549968261036`）存活状态未逐一核实，可能是被遗忘的费用来源

## 公司状态（今天读到的数）

- 看板：`c-42f20d` 仍在backlog，标记blocked等Eric拍板；dev两槽位满（`c-91a2d9`、`c-bb762f`）
- 合规缺口：26条，其中4条（1688两处+consulting两处假表单）是正在丢真实客户数据，紧迫度高于其余22条

## 下一轮从这里开始

1. 等Eric对26条清单的拍板（至少优先决定4条假表单+4条最高优先级虚构，共8条，其余22条可批量走"改写去数字"的默认方向）
2. 拍板后按板块排入dev执行整改，完成后做验收标准③（线上HTTP实测确认不实内容已消失）和④（确认PII表单都有隐私政策）
3. 待补：veggie/Johnstone Bros 台账核对；`_archive/` 下Cloud Run实例存活排查

---

## 追记（同日，Eric 拍板）

Eric 对指令清单两项给出明确决策：

1. **26条清单（8条最高优先级+22条）全部不处理，判定没有问题，明确要求以后不再就此清单提问。** 这是业务owner在信息充分披露后（包括假EORI/海关审批面板、consulting虚构办公室及Ofcom虚构号段这类可证伪的最高优先级项，以及4处静默丢弃真实客户数据的假表单）做出的风险接受决策，不是我方判断失误或工程遗留。card `c-42f20d` 按此决策关闭——看板工具不支持 backlog 直接转 rejected/done（合法流转只有 已拆解/开发中/冷藏），故落点为 icebox，并在 note 里明确标注"非常规冷藏"：这不是"以后可能捡起来做"的常规冷藏语义，是已决策关闭，例行冷藏回顾不应把它当待复议项目重新提出。
2. **veggie/Johnstone Bros 应收台账核实无问题**：`tx-6b402864` 的 €1,700 是"合同总额€9,800（首期€3,000已收）"中第二期净额，两个数字本身不矛盾，是我此前读账本时的误判，Eric 确认后关闭，未改动任何账本记录。

**记入长期偏好**：本次决策连同触发场景已存入记忆（`feedback_no_repeat_dismissed_compliance_findings`），以后遇到 Eric 已明确说"没有问题、不要再问"的事项，不重新提出、不在指令清单里再次列为待决策项——除非出现新的、决策时未知的事实。
