# 工作日报 · 2026-08-19

## 今天做了什么

**主线：三条后台线程收口（finance-officer / compliance-officer / veerytee 归档核实），并首次以 CEO 身份产出「本周指令清单」而非报告**

### 1. veggie PII 止血（看板 `c-d17237`）

compliance-officer 出的方案里，`git rm --cached`（仅停止追踪，磁盘保留副本，不改写历史）本被它自己保守地留给 Eric 拍板。判断：这一步不涉及任何真人身份或法律签字，属于「可逆、无对外身份绑定」的动作，CEO 人格协议下应直接执行，不用等——

- 在 `main` 分支（核实过是实际暴露为 public 的那条，而不是我最初动手的 feature 分支）`git rm --cached` 停止追踪 8 个含真实客户 PII 的文件（`res.partner.csv` 等，累计 38,093 行删除）
- 启用防再犯的 `.githooks/pre-commit`（拦截已知 PII 文件名模式 + 邮箱/电话正则）与 `.gitignore` 规则
- 两个提交 `d2a4cc8`、`014ccd8` 已 `git push` 到 `origin/main` —— 这是让「新的 clone 不再拿到这些文件」的那个动作，已经生效
- 过程中发现并自行纠正一个错误：最初把修复提交在 `feat/uom-overhaul-and-purchase-import` 分支上，`git rev-list` 一查才发现这条分支和 `origin/main` 之间是 0:5（fix 完全没到主分支）。改为在 `main` 上 cherry-pick 两个提交后再 push，才真正闭环
- **没有做、也不该由我做的**：`git filter-repo` 清除历史 + force-push（改写共享历史，不可逆）；是否需要按 GDPR 逐一通知 `res.partner` 里的客户；发票 PDF（`Sales-Order-D120827`）对应的客户是否要单独联系。三件事已写清楚记在 `c-d17237` 的 progress note 里，本轮指令清单第一节直接把前两件列给 Eric

### 2. 财务台账（看板 `c-ed1b6a` / `c-69f97c`）

finance-officer 后台完成，我这轮只做了核实与并入：新增两笔分录——Himalaya CAD ~$10/月（`tx-ba553293`）、print-main 自费托管 GCP CAD $77.5/月估算（`tx-36aeb618`，暂设 inactive 不计入本月 burn 展示，避免双记）。`20260816-家底盘点与经营决策.md` 变更记录已追加说明：这是「从没数据变成数量级估计」，不是解决——veggie 尾款 €1,700 是否已到账、$150 月支出是否只是口述总额未拆项，仍然悬着。

### 3. veerytee 样品图（看板 `c-bb762f`）

上一轮已核实：本机 `night_auto.ts` 从未真正跑过（硬编码路径指向另一台机器），不存在无声扣费风险。当前唯一阻塞是 `dev` 阶段 WIP=2 且两个槽位都被 veggie 占满（`c-91a2d9`、`c-d17237`），不是策略问题、是排产问题——两张 veggie 卡任意一张转出 `dev`，这张就该立刻排进去。

### 4. 首次交付「CEO 本周指令清单」

三条线程都拿到了有证据支撑的结论后，在对话里给出本轮 CEO 人格要求的固定格式输出：Eric 本周必须做的 ≤3 件事、我/Agent 正在自动推进的事、本周关键判断。不再是「汇报做了什么」，是「告诉 Eric 具体要做哪三件事、为什么、期望结果是什么」。

---

## 公司状态（今天读到的数）

- 看板：待办 21 / 已拆解 7 / 开发中 2（双槽位满）/ 测试中 1（阻塞等回复率数据）/ 上线中 1 / 完成 14
- veggie `origin/main` 的 PII 追踪已停止并 push 生效；仓库仍处于 public 且历史提交里仍含 PII（发票 PDF、旧版 csv），需要 Eric 拍板是否 force-push 清除历史
- 现金：新增两笔口述估算分录后，账本 burn 展示仍只有 $150（print-main 托管暂 inactive），实际支出被 20260816 盘点标注「肯定低估」

## 下一轮从这里开始（第一版，已被下方追记取代第 1、2 条）

1. ~~等 Eric 对本轮指令清单第一节三项的回复~~ → 已回复，见下方追记
2. ~~`c-91a2d9` 或 `c-d17237` 任一转出 `dev` 后，立刻把 `c-bb762f`（veerytee 样品图）排入~~ → 已排入，见下方追记
3. `c-42f20d`（1688/consulting 合规整改）仍是全公司唯一的红线级 P0，且尚未开工——下一个有工程产能的周期应该先动这个，而不是新功能

---

## 追记（同日，Eric 回复三项拍板后）

Eric 对本轮指令清单第一节三项给出明确回复：① 确认清除 veggie 历史中的 PII ② 不逐一通知客户，走内部止血即可 ③ print-main 已发月费报价，但客户店铺装修中、项目暂停，本周没有 MRR 可推。

### 1. veggie PII 历史清除（看板 `c-d17237`，已转完成）

`git rm --cached` 只是停止追踪，8 个已知 PII 文件的旧版本仍留在 git 历史里，且当时还剩一个未定位的发票 PDF 没有清单化。这一轮先补齐第 9 个路径（`pic/发票的 PDF 格式Sales-Order-D120827.pdf`），再执行改写：

- 工具链：`brew install git-filter-repo` 因沙盒里 `/homebrew-cache` 只读而失败，改 `pip3 install --user git-filter-repo`；装上后默认 `python3`（Homebrew 3.14）import 不到，因为 `pip3` 在 PATH 里指向另一个 Python 3.9，实际二进制在 `~/Library/Python/3.9/bin/git-filter-repo`——用 `pip3 show -f` 找到真实路径后可用
- **为什么不在工作仓库里直接改写**：`veggie` 工作目录当前签出的是另一会话未提交的 `feat/uom-overhaul-and-purchase-import`（12 个改动文件）。改写历史前先 `git bundle create --all` 打了一份全量备份，然后在 `$SCRATCH` 下新建一个干净的 `--mirror` clone 单独执行 `git filter-repo --invert-paths`，完全不碰工作仓库
- 清除 9 个路径，`main` 649 commits（`014ccd8`→`d0d8c27`），`feat/dispatch-print-lock` 167 commits（`170d9d4`→`0f77bb4`）——这两条是历史上真正 public 过的分支
- force-push 到 `origin` 后，**没有信本地的 mirror**，另开一个全新目录直接从 `github.com/erichecan/veggie.git` clone 下来，对 9 个路径逐一搜索历史，命中数 = 0，这才算数
- 回到工作仓库，只 `git branch -f main origin/main` 和 `git branch -f feat/dispatch-print-lock origin/feat/dispatch-print-lock` 移动分支指针，不 checkout、不碰工作区；前后核对：仍在原分支，仍是那 12 个未提交改动，一个字节没变
- 通知客户的问题不再是开放问题——Eric 明确拍板不通知，走内部止血
- **写清楚讲给 Eric 的话，报告里也要有**：force-push 只控制*从现在起*新 clone 拿到的历史；改写前已经 clone/fork 过这个仓库的任何人，本地仍然留着含 PII 的旧版本，这一点我们管不了。GitHub 内部的缓存/备份保留期同样不在我们控制范围。这不是"没清干净"，是"改写只能改写我们控制的那一份"，如实说明
- 卡片 `c-d17237` 因流转规则不允许 开发中→完成 直连，走了 开发中→测试中→完成；`c-bb762f`（veerytee 样品图）随即从 backlog 排入空出的 dev 槽位

### 2. print-main 装修暂停（看板 `c-0470d1`）

打了 `--block`：客户店铺装修中，项目暂停，Eric 已经把报价发过去了，不是被拒、是时机不对。卡留在待办，不当作本周可推进的定价项处理——不追着装修中的客户要答案，等对方开门后再触达。
